version: 0.2

phases:
  install:
    commands:
      - echo Installing packages
      - pip3 install flask gunicorn

  build:
    commands:
      - echo Build successful

artifacts:
  files:
    - '**/*'
